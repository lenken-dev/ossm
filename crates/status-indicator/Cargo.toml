[package]
name = "status-indicator"
version = "0.1.0"
edition = "2024"

[features]
policy = ["dep:ossm", "dep:pattern-engine"]

[dependencies]
smart-leds = "0.4"
embedded-hal = "1.0"
ossm = { path = "../../ossm", optional = true }
pattern-engine = { path = "../pattern-engine", optional = true }

[[test]]
name = "policy"
required-features = ["policy"]
